# string-reverser

Reverses a string character by character, correctly handling Unicode surrogate pairs and special characters.

## Requested task

Create a deterministic offline tool that accepts {"text":string} and returns the characters in reverse order as a string. Include edge-case tests.

This generated tool is executed only inside a network-denied Vercel Sandbox.
