# json-canonicalization-scheme

Implements RFC 8785 (JCS) to produce a deterministic, canonical string representation of any JSON-compatible object.

## Requested task

an offline tool to canonicalize JSON according to RFC 8785 (JSON Canonicalization Scheme)

This generated tool is executed only inside a network-denied Vercel Sandbox.
