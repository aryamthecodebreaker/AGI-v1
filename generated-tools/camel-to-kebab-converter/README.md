# camel-to-kebab-converter

Converts all camelCase keys in a JSON object to kebab-case, preserving values and handling nested objects.

## Requested task

Convert a supplied JSON object's camelCase keys to kebab-case without changing values

This generated tool is executed only inside a network-denied Vercel Sandbox.
