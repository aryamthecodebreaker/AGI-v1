# clamp-number

Clamp a finite numeric value between finite minimum and maximum bounds and return the clamped number.

## Requested task

Clamp a finite numeric value between finite minimum and maximum bounds and return the clamped number

This generated tool is executed only inside a network-denied Vercel Sandbox.
