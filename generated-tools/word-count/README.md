# word-count

Counts whitespace-separated words in a string.

## Requested task

Create a word-count tool that returns the number of whitespace-separated words in a string.

This generated tool is executed only inside a network-denied Vercel Sandbox.
